const expect = require('chai').expect;

describe('Unit Test', () => {
  it('Should work - unit test', () => {
    const a = 2;
    const b = 2;
    const sum = a + b;
    expect(sum).to.equal(4);
  });
});
